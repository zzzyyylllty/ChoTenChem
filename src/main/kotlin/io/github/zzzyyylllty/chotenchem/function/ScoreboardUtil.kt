package io.github.zzzyyylllty.chotenchem.function

import org.bukkit.entity.Player


class ScoreboardUtil {
    fun sendScoreBoard(p: Player, isPacketEventsMode: Boolean) {
        if (isPacketEventsMode) {

        } else {

        }
    }
}